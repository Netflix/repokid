language: python

matrix:
  include:
    - python: "2.7"

install:
  - pip install pytest
  - pip install bandit

script:
  - pip install -r test-requirements.txt
  - pytest
  - bandit -r -ll -ii -x tests/

after_success:
  - coveralls

notifications:
  email:
    tmcpeak@netflix.com
    pkelley@netflix.com
